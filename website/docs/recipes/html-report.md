# Generate HTML Reports

Executing `stacktest run` automatically compiles event logs into a portable interactive HTML report dashboard:

```text
Reports generated:
  HTML:  /path/to/my-project/.stacktest/runs/st-run-abc/report.html
```
