# Troubleshooting

### AWS Credentials Not Found
Verify your AWS profiles are loaded and configured in `~/.aws/credentials` or environment variables are set.

### Template Validation Errors
Verify your configuration template paths exist and point to valid files.
