# Project Configuration

The `project` block defines the top-level name of your testing workspace.

```yaml
project:
  name: billing-engine
```

- **`name`** *(required)*: String. Must be alphanumeric with hyphens, lowercase, and cannot contain spaces. This name is used to scope staging resources and ownership tags.
